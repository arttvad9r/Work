# Room schemas

Room schema export is configured to this directory. The initial `1.json` is generated by KSP during a verified Android build and must be committed before release. Every future database version must retain prior schemas and include migration tests.
