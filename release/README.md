# Release identity

The public production signing certificate fingerprint is stored in `production-signing-cert-sha256.txt` and is enforced by the release tooling. Never store the private signing key or passwords in this repository.
